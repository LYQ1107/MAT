# 姿态与身份补标计划

在真实群体视频到位后，导出中性片段 UID、原始时间、物种骨架和可见性规范；随机连续片段与预测困难片段按固定抽样概率并列。每个选定群体覆盖多个日期，每个日期抽取连续 5–10 秒片段，实际数量按动物数、FPS 和可用标注工时估算并写入 manifest。连续片段内逐帧标实例 ID、关键点和遮挡/未知，不把插值预测当 GT；争议帧独立复核。

H-human 身份核验权限只覆盖 S0；后续姿态/身份评价标签不进入 memory 或 checkpoint 选择。没有真实操作时保持 `BLOCKED_NEEDS_REFERENCE_REVIEW`，不填写人工分钟数。

